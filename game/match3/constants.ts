import { BasicColor } from './types';

export const BOARD_ROWS = 8;
export const BOARD_COLS = 8;
export const COLORS: BasicColor[] = ['red', 'blue', 'green', 'yellow', 'purple'];
export const BASE_TILE_SCORE = 10; // base score per tile cleared
export const LEVEL_TARGET_SCORE = 2000;
export const LEVEL_MOVES = 30;
